/**
 * Chat modes module exports.
 *
 * Provides chat mode implementations and utilities for the strategy pattern
 * architecture. Chat modes control how agents are enabled/disabled during
 * conversations, supporting different interaction patterns while maintaining
 * a consistent interface.
 *
 * @module chat-modes
 */

export { ManualChatMode } from "./ManualChatMode";
