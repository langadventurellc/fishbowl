import { BaseSelectProps } from "./BaseSelectProps";

export interface PersonalitySelectProps extends BaseSelectProps {
  // PersonalitySelect extends BaseSelectProps but doesn't add any additional props
}
