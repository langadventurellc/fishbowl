/**
 * Props for theme preview component
 *
 * @module types/ui/ThemePreviewProps
 */

import { ThemeMode } from "@fishbowl-ai/shared";

export interface ThemePreviewProps {
  selectedTheme: ThemeMode;
}
