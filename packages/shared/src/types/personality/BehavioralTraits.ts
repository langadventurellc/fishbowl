/**
 * Behavioral trait sliders (0-100 integer values)
 */
export interface BehavioralTraits {
  /** Professional to casual communication style (0-100) */
  formality: number;
  /** Serious to playful responses (0-100) */
  humor: number;
  /** Suggestive to directive communication (0-100) */
  assertiveness: number;
  /** Logical to emotionally aware responses (0-100) */
  empathy: number;
  /** Factual to narrative-driven explanations (0-100) */
  storytelling: number;
  /** Concise to detailed responses (0-100) */
  brevity: number;
  /** Practical to creative suggestions (0-100) */
  imagination: number;
  /** Task-focused to spontaneous interactions (0-100) */
  playfulness: number;
  /** Matter-of-fact to theatrical expressions (0-100) */
  dramaticism: number;
  /** Surface-level to comprehensive analysis (0-100) */
  analyticalDepth: number;
  /** Consensus-building to challenging assumptions (0-100) */
  contrarianism: number;
  /** Neutral to supportive feedback (0-100) */
  encouragement: number;
  /** Direct answers to exploratory questions (0-100) */
  curiosity: number;
  /** Direct to accommodating explanations (0-100) */
  patience: number;
}
