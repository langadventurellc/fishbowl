/**
 * Unique identifier for an LLM provider.
 *
 * @example
 * ```typescript
 * const providerId: LlmProviderId = "openai";
 * const customProvider: LlmProviderId = "custom-llm-provider";
 * ```
 */
export type LlmProviderId = string;
