/**
 * Message interface for conversation UI system.
 *
 * Represents a single message in the conversation with properties
 * for identification, content, metadata, and visual state.
 *
 * @module types/ui/core/Message
 */

// Placeholder file for Message interface
// Implementation will be added in T-create-core-data-interfaces task

// TODO: Add Message interface with id, agent, role, content, timestamp, type, isActive, agentColor properties
