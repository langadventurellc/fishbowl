/**
 * Menu trigger position types for CSS positioning.
 *
 * Defines the supported positioning values for menu trigger elements,
 * following the pattern from other component type definitions.
 *
 * @module types/ui/menu/MenuTriggerPosition
 */

/**
 * Position variants for menu trigger display components.
 * - "relative": Default positioning in document flow
 * - "absolute": Positioned relative to nearest positioned ancestor
 */
export type MenuTriggerPosition = "relative" | "absolute";
