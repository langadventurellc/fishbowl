// This file has been removed as part of the migration to Zustand state management.
// ThemeProvider is no longer needed as theme state is now managed by Zustand store.
// Theme state is automatically available to all components through the store.

// Removed: ThemeProviderProps interface
// Migration: No provider wrapper needed, use useTheme hook directly
